/**
 * All project constants
 * @module utils/constants
 */
const fs = require('fs');
const util = require('util');
module.exports = {
  messages: {
    info: {
    },
    error: {
    }
  },
  regex: {
  },
  values: {
  },
  routes: {
    options: {
      onProxyRes: (proxyRes, req, res) => {

      }
    }
  }
};
